name: Hello World CI

on:
  push:
    branches: [ main ]
    paths:
      - 'ressources/licensetool/**'

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print Hello World
        run: echo "👋 Hello World from ressources/licensetool!"
